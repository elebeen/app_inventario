name: registro_productos
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.4.0  # ✅ recomendado, ya que Flutter estable actual trabaja con 3.4.x

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  mobile_scanner: ^7.1.3  # ✅ escáner que sí funciona sin conflictos
  provider: ^6.1.0
  shared_preferences: ^2.2.0
  dio: ^5.9.0
  get_it: ^8.2.0
  flutter_dotenv: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
